{
  "editor": "mvim",
  "command": {
    "ruby": "docker-compose run cmd ruby",
    "test": "docker-compose run cmd ruby ./test/latex_eval_test.rb",
    "build": "docker-compose build",
    "t": "docker-compose run cmd rake test"
  }
}
